---
title: ".agentignore"
description: "Metadata for the .agentignore file, detailing agent access restrictions."
target_file: .agentignore
source: generated
generated: 2026-07-03
model: jules-model
version: 0.1.0
status: final
---

### Architectural Intent
The `.agentignore` file establishes a strict boundary for AI agents (such as Jules) operating within the Rotkeeper ecosystem. It explicitly prevents agents from accessing, reading, or modifying protected directories, such as compiled book reports (`bones/book-reports`) and other sensitive system paths. This ensures that autonomous operations do not inadvertently destroy human-crafted lore or break generated artifacts.

### Directory / File Schema Expectations
Located at the root of the repository (`.agentignore`), it utilizes a standard newline-delimited path format similar to `.gitignore`. AI agents are contractually obligated to parse this file and omit listed directories from their working context.

### Restless Spirits
This is a soft barrier. It relies entirely on the AI agent's tooling to honor the ignore list. A rogue or poorly configured agent could easily bypass this and obliterate protected files.

### Ritual Warnings
Never put critical security keys here expecting them to be safe; this only stops well-behaved scripts, not determined extraction.
