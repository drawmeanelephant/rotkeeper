---
title: "Showcase: brutal — Unix Green"
slug: "showcase-brutal-unix"
template: "theme-brutal.html"
palette: "unix"
description: "Programmatic description for brutal-unix"
---

## Terminal, tuned

Every demo page on this slice renders **the same body** through the three
palettes — only the token swap: `mac`, `unix`, and `pwsh`.

```sh
$ rotkeeper.sh render
Exorcism complete.
```

> Swap the palette in one line of frontmatter. No JS, no CDN, no rebuild of
> the pipeline — the brutalist mechanics stay intact.